[phases.setup]
nixPkgs = ['nodejs', 'npm', 'python3']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run prisma:generate', 'npm run build']

[start]
cmd = 'npm run start'

[variables]
NODE_ENV = 'production'
PORT = '3000' 